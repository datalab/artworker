require "spec_helper"

describe Artworker::Dimensions do
  
  it "should return a the dimensions as a fraction"
  
  it "should return a the dimensions as a decimal"
  
  it "should return both the regular and framed dimensions"
  
end